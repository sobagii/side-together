{
  "name": "ger",
  "version": "1.0.0",
  "private": true,
  "description": "Private idea Ger for side-project teams",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
