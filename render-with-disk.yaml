services:
  - type: web
    name: ger
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
      - key: DATA_DIR
        value: /var/data
      - key: INVITE_CODE
        sync: false
    disk:
      name: ger-data
      mountPath: /var/data
      sizeGB: 1
